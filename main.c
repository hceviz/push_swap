#include "pushswap.h"


int	main(int ac, char **av)
{
	if (ac != 1)
	{
		int	x;
		int	index;

		x = 1;
		index = 0;
		if (av[x])
		{
			Stack *new;
			new->prev = NULL;
			new->value = ft_atoi(av[x]);

		}
		while (av[x])
		{

		}
	}
}